# Default repository to query (fallback if backend is unavailable)
VITE_DEFAULT_REPOSITORY=yadnyeshkolte/ContextKeeper

# Default branch
VITE_DEFAULT_BRANCH=main

# In production (Vercel), the API URL is handled automatically by the api utility
# In development, set this to your local backend
# VITE_API_URL=http://localhost:3000
