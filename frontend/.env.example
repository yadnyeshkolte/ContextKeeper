# Default repository to query (fallback if backend is unavailable)
VITE_DEFAULT_REPOSITORY=yadnyeshkolte/online-voting-system

# Backend API URL
VITE_API_URL=http://localhost:3000

# Default branch
VITE_DEFAULT_BRANCH=main
